#include <bits/stdc++.h>

#define make_pair mp

using namespace std;

void get_ans(int n)
{
    int a[n];
}

int main()
{
    freopen(".in", "r", stdin);
    freopen(".out", "w", stdout);
    int n;
    while (scanf("%d", &n) == 1)
        get_ans(n);
    return 0;
}